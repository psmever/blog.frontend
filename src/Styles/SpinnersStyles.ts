import styled from 'styled-components';

export const BodyLoading = styled.div`
    margin: 4;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    height: '100vh';
    display: 'flex';
    margin-top: 50%;
    display: block;
    margin: 0 auto;
    text-align: center;
    border-color: red;
`;

export const ButtonSpinnerStyle = styled.div`
    /* width: 100%; */
`;
